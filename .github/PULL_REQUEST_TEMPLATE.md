## Description

What does this change do?

## Motivation

Why is it needed?

## How was it verified?

The commands or tests you ran, and any new tests or fixtures you added.

## Related issue

<!-- e.g. "Closes #123". Link the issue this addresses, if any. -->

## Checklist

These are the exact steps CI runs, so a clean local run means CI will pass:

- [ ] `cargo fmt --check` passes
- [ ] `cargo clippy -- -D warnings` passes
- [ ] `cargo build` passes
- [ ] `cargo test` passes
- [ ] Behavior changes are covered by a test that fails before the change and passes after
