# A manifest that contradicts the exported spec of the fixture WASM.
#
# The fixture exports `ConfigData { admin: String, threshold: u32 }`. This file
# claims the opposite field order. Acting on a declaration that disagrees with
# the contract would certify a layout the contract does not use, so this must be
# rejected outright rather than analyzed.

[[value_type]]
name = "ConfigData"
kind = "struct"

  [[value_type.field]]
  name = "threshold"
  type = "u32"

  [[value_type.field]]
  name = "admin"
  type = "String"
